export interface Instructor {
    id: number;
    name: string;
    bio: string;
    specialties: string[];
    profile_picture_url: string;
    rating: number;
    instrument_ids: number[];
  }
